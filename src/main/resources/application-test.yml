spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true

jwt:
  secret: mySuperSecretKeyForTestingOnly123456789
  access-token-expiration: 15m
  refresh-token-expiration: 7d

cloudinary:
  cloud-name: test
  api-key: test
  api-secret: test